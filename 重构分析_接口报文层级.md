# 层级重构分析报告：系统-模块 → 接口-报文

> 背景：甲方不认可「系统-模块-接口-字段-数据」层级，只认「接口-报文-数据」。报文字段界面已改造完成，本报告盘点其余界面需要重构的部分、界面变化点，以及可清理的遗留代码。

## 一、两种层级模式的实现

| 模式 | 实现组件 | 树结构 | 状态 |
|---|---|---|---|
| 旧：系统-模块-叶子 | `SystemModuleTree.vue`（通用 IDE 树，内置系统/模块增删改） | 系统 → 模块 →（字段/报文/数据集/规则集…） | 仍被 6 个界面使用 |
| 新：接口-报文-数据 | `MonitorTree.vue`（接口收发监控树）、`DatasetTree.vue`（测试数据树） | 顶级分组：系统接口 / 方案 / 自定义接口；接口直挂报文（1:N 排他归属）→ 字段/数据集 | 3 个页面已改造 |

**已改造页面（重构参考模板）**：

1. `Protocol.vue` 报文字段管理 → MonitorTree：接口节点徽标「N 报文」，接口详情卡展示名下报文列表，报文节点展开编辑字段。
2. `InterfaceMonitoring.vue` 接口收发监测（路由 `/execution`）→ MonitorTree：三个分组 + 右键「加入发送/接收监控」。
3. `TestData.vue` 测试数据管理 → DatasetTree：系统接口 → 接口 → 报文 → 数据集 / 数据集方案 / 未关联报文。

## 二、待重构界面明细

### 1. 首页 `views/Dashboard.vue`（在用，旧树）
- **需改部分**：① 顶部「系统联试状态」卡片区（按系统分组，指标含「在线模块/模块数」）；② 左侧快捷收发树（`SystemModuleTree`，系统→模块→接口/方案）。
- **界面变化**：
  - 卡片区从「按系统」改为「按接口/报文就绪度」口径，去掉「在线模块」指标（或改为「报文已配置/待配置」）。
  - 左侧树换 `MonitorTree`（系统接口/方案/自定义接口），搜索框文案改「搜索接口 / 报文」。
  - 快捷测试目标从「接口/方案」扩展为可直接选中报文，跳转 `/execution` 时携带报文定位。

### 2. 校验规则管理 `views/Rule.vue`（在用，旧树）
- **需改部分**：左侧树（`SystemModuleTree`，模块→规则集）；`createRuleSet` 依赖 `moduleId`；从字段页跳转逻辑按「同模块」找规则集。
- **界面变化**：
  - 树改为「接口 → 报文 → 规则集」（规则集归属报文，或挂在接口下聚合），新建规则集时不再选择模块。
  - 「创建规则集」按钮改为基于当前选中的接口/报文；跳转联动（`/protocol` 与 `/rule` 互跳）改为按接口/报文 id 定位，去掉 `systemStore.setCurrent` 与模块定位。

### 3. 异常数据管理 `views/Exception.vue`（在用，旧树）
- **需改部分**：左侧树（`SystemModuleTree`，仅作系统/模块筛选）；`scopeTitle`（「××系统 · 全部模块」）；`filtered` 按 systemId/moduleId 过滤。
- **界面变化**：
  - 树换 `MonitorTree`（只读模式），按接口/报文筛选异常；scope 文案改「××接口 / 全部接口」。
  - 过滤条件从 systemId/moduleId 改为接口维度；「查看全部模块」按钮改「查看全部接口」。

### 4. 统计与可视化 `views/Statistics.vue`（在用，无树但依赖模块）
- **需改部分**：顶部筛选「模块」下拉（`connStore.modulesOf`）；`utils/statsAggregator.js` 大量按 `moduleId` 聚合（运行总览、异常分布、接口观测等 10+ 处）；导出 CSV 列含「模块」。
- **界面变化**：
  - 筛选从「模块/接口」两级改为「接口/报文」或「方案」维度。
  - 图表「按模块分布」改为「按接口分布」；导出列「模块」改「接口/报文」。

### 5. 链路连接管理 `views/Connection.vue`（在用，系统/模块源头）
- **需改部分**：整个页面定位需重新决策——它是唯一管理系统（`SystemManager`）与模块（拓扑图、模块参数 IP/端口/在线状态）的页面；接口上仍保留 `systemId/moduleId` 字段。
- **界面变化（两种方案）**：
  - 方案 A（推荐）：保留页面但弱化层级——树去掉「系统」概念或平铺为「接口链路」列表；模块参数收敛为「接口链路参数」，不再作为导航层级。
  - 方案 B：整体下架该页面，接口链路参数并入接口配置弹窗（`InterfaceQuickConfig` 已有传输配置），删除 `SystemManager`/拓扑。

### 6. 顶栏 `layouts/MainLayout.vue`（全局）
- **需改部分**：「被测系统」下拉（`systemStore.options`）+「管理」按钮（`SystemManager`）；全局 `currentId` 过滤联动（Dashboard/Exception/Statistics 等页面都 watch `systemStore.currentId`）。
- **界面变化**：
  - 顶栏下拉改为「接口/报文」或移除；「管理」按钮移除或改为「接口配置」入口。
  - 删除 `systemStore.currentId` 的全局过滤联动，各页面独立管理选中态。

## 三、遗留代码清单

### 3.1 无任何引用的组件（grep 全项目确认，可安全清理）

| 文件 | 说明 |
|---|---|
| `components/ModulePlaceholder.vue` | 无引用 |
| `components/FieldNode.vue` | 仅自递归，无外部引用 |
| `components/protocol/GrpcConfigForm.vue` | 传输类型已改为 OSE/4908A/MDS，gRPC 表单废弃 |
| `components/protocol/HttpConfigForm.vue` | 同上，HTTP 表单废弃 |
| `components/protocol/ProtocolTypeDialog.vue` | 协议类型选择弹窗，无引用 |
| `components/protocol/SignaturePreview.vue` | 无引用 |
| `components/protocol/RepeatGroup.vue` | 无引用（注意：store 里的 `makeRepeatGroup` 函数仍被 ByteFieldTree 使用，勿删） |
| `components/protocol/FramingPanel.vue` | 报文头配置面板，无引用 |
| `components/protocol/StructFieldEditor.vue` | 无引用（注意与仍在用的 `StructNestedEditor.vue` 区分） |

### 3.2 不在路由中的页面（重构完成后可清理）

| 文件 | 状态 |
|---|---|
| `views/Execution.vue` | 已被 `InterfaceMonitoring.vue` 取代，路由 `/execution` 指向新页 |
| `views/Reception.vue` | `/reception` 路由已重定向到 `/execution?mode=receive` |
| `views/TestTask.vue` + `components/testtask/TaskBindings.vue`、`CreateTaskDialog.vue` | 测试任务页无路由入口；**但 `stores/testTask.js` 仍被 execution/rule/InterfaceMonitoring 等使用，store 不能删** |

### 3.3 重构完成后将失去引用的代码（先重构再清理）

| 文件 | 说明 |
|---|---|
| `components/SystemModuleTree.vue` | 6 个界面替换完 MonitorTree/DatasetTree 后失去引用 |
| `components/SystemManager.vue` | MainLayout/Connection 的系统管理弹窗，若系统概念下架则失去引用 |
| `stores/system.js`（`systemStore`） | 20+ 文件引用；重构后仅剩数据模型上的 systemId 归属，可考虑简化为纯数据源 |
| `composables/useEntityNameGuard.js` 中的系统/模块命名 | 随系统/模块管理下架后收敛 |

### 3.4 不建议删除（仍在用 / 属资源）

- `src/mock/`（seed-data.js / testData.js / users.js）——被 10 个 store 作为初始化数据源，属于演示数据底座。
- `src/stores/`（testTask、planScheme、runBatch、customIface 等）——除 testTask 外均在用。
- `src/utils/`——offsetCalc / receiveValidator / dataGen / ruleEngine / statsAggregator / knowledgeQuality / excellentHeat 等均有引用。
- `src/services/testDataService.js`——上传/导出服务，多处引用。
- 截图留档 `.playwright-mcp/shots/`、`docs/` 历史文档——留底证据，勿清。

## 四、清理建议（分级，均在安全范围内）

1. **A 级 · 可立即清理（无引用、删后不影响构建）**：3.1 全部 9 个组件 + 3.2 的 `TestTask.vue`/`TaskBindings.vue`/`CreateTaskDialog.vue`（页面级无路由）。
2. **B 级 · 重构完成后再清理**：3.3 的 SystemModuleTree / SystemManager / systemStore 收敛 / Execution / Reception。
3. **C 级 · 不清理**：3.4 全部；以及 `.workbuddy/` 项目数据目录。
4. **临时文件**：`.playwright-mcp/` 根目录的 `page-*.yml` / `console-*.log` 属临时缓存，可随时清理；`dist/` 为构建产物，可安全重建。

> 安全约束已遵守：以上清理建议不涉及任何资源文件（seed 数据、截图、文档、样式、图标）的删除；执行删除前会逐项列清单并确认。
