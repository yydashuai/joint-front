# 接口树分层展示设计

> 目标：所有界面共用同一棵「接口-报文-数据」层级模型，但**按界面职责截取不同深度**——树的末端就是该界面的操作对象，中间层仅作导航，无关层不出现。

## 一、设计原则

1. **单一模型，多界面视图**：数据层只有一棵树（接口 → 报文 → 数据集/规则集），界面不各自造树，只声明"看到第几层"。
2. **末端即职责**：每棵树的最后一级 = 该界面真正要操作的对象（编辑字段 / 加入计划 / 编辑数据 / 编辑规则 / 筛选范围）。
3. **能力可配置**：分组显示、徽标、右键动作、新建入口、只读/可编辑，全部由界面通过配置注入，树组件保持通用。
4. **筛选型与编辑型分离**：只做范围筛选的界面（异常、统计、首页）不提供新建/删除能力。

## 二、完整层级模型（所有界面共用的"全景树"）

```
顶级分组（3 个，可裁剪）
├─ 系统接口 ─ 接口 ─ 报文（1:N 排他归属）─ 字段（protocolRefs）／数据集（messageId 关联）
├─ 方案 ─ 方案内接口（系统接口可继续展开报文；自定义接口为密文叶子）
└─ 自定义接口（密文叶子，无报文/字段）
```

分层编号：L0 顶级分组 · L1 接口/方案/自定义接口 · L2 报文 · L3 数据集/规则集（界面按需挂载）

## 三、各界面深度矩阵

| 界面 | 类型 | 分组 | 接口 | 报文 | 数据集 | 规则集 | 方案 | 末端（操作对象） |
|---|---|---|---|---|---|---|---|---|
| 报文字段管理 | 编辑型 | ✓ | 节点 | **末端** | — | — | 节点 | 报文（编辑字段） |
| 接口收发监测 | 操作型 | ✓ | 节点 | **末端** | — | — | 节点 | 报文（加入收发计划） |
| 测试数据管理 | 数据型 | ✓ | 节点 | 节点 | **末端** | — | 节点 | 数据集（编辑数据） |
| 校验规则管理 | 规则型 | ✓ | 节点 | 节点 | — | **末端** | 节点 | 规则集（编辑规则） |
| 异常数据管理 | 筛选型 | ✓ | 节点 | **末端** | — | — | — | 报文（限定筛选范围） |
| 统计与可视化 | 维度型 | — | **维度** | — | — | — | **维度** | 接口/方案（下拉维度） |
| 首页 Dashboard | 入口型 | ✓ | **入口** | — | — | — | **入口** | 接口/方案（跳转快捷测试） |
| 链路连接管理 | 链路型 | — | — | — | — | — | — | 独立：系统→模块物理链路 |

## 四、各界面树形态详述

### 1. 报文字段管理 `Protocol.vue`（已按此实现，作为模板）
- 树：MonitorTree（editor-mode），分组 = 系统接口/方案/自定义接口。
- 末端 = **报文**：点击报文 → 右侧打开字段编辑器（InterfaceEditor）；接口节点点击 → 详情卡（名下报文列表），不打开编辑。
- 徽标：接口「N 报文/未配置报文」；报文「传输类型·N字段 / 文件·类型」。
- 新建：仅「系统接口」分组提供 +接口；方案/自定义接口分组隐藏添加按钮（editor-mode）。
- 右键：报文 = 编辑/删除/生成校验规则；接口 = 配置接口/添加报文/删除。

### 2. 接口收发监测 `InterfaceMonitoring.vue`（已按此实现）
- 树：MonitorTree（操作模式），三个分组全量。
- 末端 = **报文 / 自定义接口**：右键「加入发送监控 / 加入接收监控 / 立即发送」，可拖拽入计划。
- 徽标：接口「N 报文」+ 触发方式；自定义「传输类型·已配置/待配置」。
- 方案为中间节点：展开显示方案内接口，支持整体拖入计划。

### 3. 测试数据管理 `TestData.vue`（已按此实现）
- 树：DatasetTree（独立树，见工作记忆 2026-08-05 约定），分组 = 系统接口/方案/自定义接口（密文不展示）。
- 末端 = **数据集 / 数据集方案 / 未关联报文（游离）**：点击 → 数据矩阵编辑器 / 方案详情。
- 接口、报文均为中间节点：接口点击仅展开，报文展开显示其数据集。
- 数据结构：报文 → 数据集（1:N），字段由 protocolRefs 解析，数据集不直接关联字段。

### 4. 校验规则管理 `Rule.vue`（待重构，需扩展新末端）
- 树：在 MonitorTree 基础上挂载 **规则集末端**（新能力，见组件方案）。
- 层级：接口 → 报文 → **规则集**（规则集归属报文）；方案节点可选展示（方案内接口的规则集聚合）。
- 末端行为：点击规则集 → 右侧规则列表；新建规则集入口 = 报文右键「生成校验规则」+ 页头按钮。
- 徽标：规则集「启用·N 条 / 草稿·N 条」。
- 跳转联动：`/protocol` ↔ `/rule` 互跳按报文 id 定位，不再按模块。

### 5. 异常数据管理 `Exception.vue`（待重构，筛选型）
- 树：MonitorTree **只读模式**（readonly），仅保留「系统接口」分组，方案/自定义接口分组裁剪掉（异常数据只来自系统接口接收）。
- 层级：接口 → **报文**（末端用于限定范围）。
- 末端行为：点击接口/报文 → 过滤下方异常样本表；scope 文案「××接口」。
- 无新建/删除/右键（只读）。

### 6. 统计与可视化 `Statistics.vue`（待重构，维度型）
- **不用树**：改为级联下拉「接口 → 报文 / 方案」，或顶部维度切换（segmented：接口/方案/时间）。
- 统计口径从 moduleId 改为接口/报文维度（statsAggregator 改造，见下）。
- 理由：统计场景是"选一个维度看分布"，树形导航反而增加操作成本。

### 7. 首页 Dashboard（待重构，入口型）
- 顶部卡片区：从「按系统 + 在线模块数」改为「接口/报文就绪状态」口径（可保留系统作为统计分组标签，但不再是导航层级）。
- 左侧树：MonitorTree 只读，**末端 = 接口 / 方案**（点击 → 快捷收发测试），报文层折叠不展示（进入监测台后再展开）。
- 快捷测试面板：选中接口/方案后显示「发送 / 接收」入口，跳转 `/execution` 携带定位参数。

### 8. 链路连接管理 `Connection.vue`（独立决策）
- 与报文数据树无关：保留「系统 → 模块」层级作为**物理链路管理**（模块 = IP/端口/在线状态），但不再作为其他页面的导航入口。
- 待定方案：A 保留本页（物理链路参数）；B 下架并合并进接口配置。**无论哪种，其余页面不再依赖模块导航。**

## 五、组件实现方案

| 组件 | 处置 | 说明 |
|---|---|---|
| `MonitorTree.vue` | **增强** | 新增 `leafKind` / `maxDepth` 配置：声明末端类型（message/dataset/rule-set）、可裁剪分组（`visibleGroups` 已有）；新增规则集末端渲染与右键动作注入 |
| `DatasetTree.vue` | 保持 | 数据管理专用树，已满足需求 |
| `RuleTree`（新） | 新增或并入 MonitorTree | 若并入：MonitorTree 支持 `leafKind='rule-set'` 时在报文下挂规则集子节点 |
| `SystemModuleTree.vue` | 废弃 | 6 界面全部替换后删除 |
| 统计维度 | 改造 `utils/statsAggregator.js` | `moduleId` 聚合改为 `interfaceId`（或同时支持），导出列「模块」改「接口」 |

**MonitorTree 增强示意**（伪代码）：
```js
props: {
  visibleGroups: ['system','scheme','custom'], // 分组裁剪
  leafKind: 'message' | 'dataset' | 'rule-set', // 末端类型
  readonly: Boolean,       // 筛选型：禁新建/删除/右键
  ifaceBadge/schemeBadge/customBadge, // 徽标注入（已有）
  extraContextActions,     // 右键扩展（已有，规则集「生成校验规则」等）
}
```

## 六、实施顺序建议

1. **Rule.vue**（规则集末端：MonitorTree 增强，收益最直接）
2. **Exception.vue**（只读 MonitorTree，改动最小）
3. **Dashboard.vue**（卡片口径 + 只读树）
4. **Statistics.vue**（下拉维度 + statsAggregator 改造）
5. **MainLayout.vue**（去掉被测系统下拉/管理入口）
6. **Connection.vue**（方案 A/B 决策后收尾，清理 SystemModuleTree/SystemManager/systemStore）

> 每个界面改造完成后跑 `vite build` 验证；全部完成后再执行遗留代码清理（见《重构分析_接口报文层级.md》清理清单）。
