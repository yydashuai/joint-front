# 便携式智能联试工具 v3.0 — 前六界面优化总结

> 日期：2026-07-01
> 范围：Login / Dashboard / Connection / Protocol / TestData / TestTask 共 6 个界面
> 关联文档：`界面优化建议_v1.0.md`
> 工作模式：每界面优化 → Playwright 走查 → 修 bug → 中文 commit（仅本地，不推远端）

---

## 1. 总体成果

| 指标 | 数值 |
|---|---|
| 提交数（本地） | 6 个 commit |
| 改动文件 | 13 个 |
| 新增行 | +449 |
| 删除行 | -52 |
| 修复 P0 Bug | 2 个（Bug-1 头像取字 / Bug-3 BrokerHealthPanel 字符串模板） |
| 修复 P0 兼容性 | 1 个（Bug-4 FramingPanel el-radio label= → value=） |
| 实施 P1 Issue | 3 个（Issue-1 系统选择器 / Issue-3 任务头 / Issue-4 协议头） |
| 实施 P2 Issue | 3 个（Issue-6 跨页联动 / Issue-7 协议结构预览 / Issue-10 保存反馈） |
| 新增基础设施 | `src/utils/bus.js` 事件总线 |

> **未推远端**：按用户要求，所有 commit 留在本地 `main` 分支（ahead of origin 6）。

---

## 2. 各界面优化明细

### 界面 1：Login 登录页（commit `f3b1c16`）

**改进**：
- 新增「演示账号」快捷登录区，4 个账号（admin / tester01-03）
  - 管理员用橙色渐变徽章，测试员用蓝色
  - 禁用的 tester02 标锁且不可点击
  - 点击自动填充用户名/密码 + 勾选「记住我」+ 弹提示
- 修复 P0 Bug-1：MainLayout 顶栏头像取最后一个字（"张管理" → "理"）
  改为取姓名第一个字（"张"）
- 引入 `src/utils/bus.js` 跨组件事件总线（EventTarget 实现，无新依赖）
- exception store 在 `capture` / `updateState` 时 emit 事件
- execution store 在 `start` / `finalize` 时 emit 事件

**Playwright 验证**：
- ✅ 4 个演示账号 chip 全部渲染正确
- ✅ 点击 tester01 自动填充 + 高亮当前选中
- ✅ 头像显示 "李 李测试"（第一个字）
- ✅ 登录跳转成功

**对应文档**：
- 视角 5 角色切换演示

---

### 界面 2：Dashboard 工作台（commit `4822a51`）

**改进**：
- 接入事件总线，顶部「系统健康概览」加「已同步 HH:MM:SS (来源)」绿色小标签
  - 5 种事件触发刷新：初始化 / 执行开始 / 执行完成 / 新异常 / 异常已处理
  - 演示时跨页操作不再需要手动 F5
- 健康概览按「异常数 > 在线模块数 > 名称」排序，异常多的系统排前面
- 修复隐藏 bug：Dashboard 原来从 `mock/seed-data.js` 常量读任务，
  execution store 修改 taskStore.tasks 后不反映到页面
  - 改为读 `taskStore.tasks`，实时反映 store 变更
  - Playwright 验证：直接改 store.tasks[0].time，列表立即重新排序到顶部
- 标题旁加「异常数多的优先」提示

**Playwright 验证**：
- ✅ 13:51:31 (初始化) 已同步 绿色标签显示
- ✅ 顶栏头像 "李 李测试" 修复生效
- ✅ 通过 Pinia 修改 task.time 触发实时刷新

**对应文档**：
- P0 Bug-1 头像取字（跨页，但与 Login 同步修）
- Issue-6 跨页数据联动

---

### 界面 3：Connection 链路管理（commit `f8d7d60`）

**改进**：
- 修复 P0 Bug-3：BrokerHealthPanel 内 inline `StatusDot` 子组件用 string template
  - Vite + ESM build 下 runtime 无法编译
  - 改为 render function（`h` 函数）实现
- 实现 P1 Issue-1（方案 A）：页面内「被测系统」从 el-select 下拉改为 chip 群标签
  - 4 个 chip 一眼展示全部可切换系统
  - 当前系统用 dark effect 高亮，其他 plain 灰
  - 点击即切换，演示比下拉更直观
  - 顶栏 select 保留作为全局入口，双向同步未破
- 摘要区保留（负责人/模块/Broker/在线数/描述/自动检测）—— 顶栏没有这些信息

**Playwright 验证**：
- ✅ 0 warnings / 0 errors（之前 1 个 ElOnlyChild warning 也消除）
- ✅ 点"综合武器管理系统"chip 后顶栏 select 同步切换
- ✅ 点"检测连通性"输出 4 个探测包结果

**对应文档**：
- P0 Bug-3 MqProbePanel 字符串模板
- P1 Issue-1 Connection 重复选择器

---

### 界面 4：Protocol 协议管理（commit `315318a`）

**改进**：
- 修复 P0 Bug-4：FramingPanel 内 3 个 `el-radio label=` 改为 `value=`
  - 跟同模块的 Http/gRPC/Mq 保持一致（其他 3 个已经用 value=）
- 实现 P1 Issue-4：ByteFieldTree 头部从 7 元素横排压缩到 5
  - 导入/导出合并到 `el-dropdown`（单按钮）
  - 类型/字节序用 `#prefix` inline 提示（"类型 TCP"/"字节序 大端..."）
  - 删除仍走 el-popconfirm
- 强化 P2 Issue-7 实时结构预览条
  - 浅蓝渐变 + 左侧主色描边
  - 数字 16px 加粗 primary 蓝
  - 新增「最后偏移」与「帧内占用 %」进度条
  - 进度条颜色按占用率变：绿 < 60 / 黄 60-90 / 红 > 90
- 实现 P2 Issue-10：保存后显示「✓ 已保存」绿色提示，2.2 秒后自动消失
- 修复 hoist 顺序 bug：markClean 用 showSavedHint 时 ref 未声明，移到前面
- 移除 el-tooltip 包裹 el-dropdown trigger 的写法（EP 报 role prop validation warning）

**Playwright 验证**：
- ✅ 0 warnings / 0 errors
- ✅ 头部 chip 切换正常
- ✅ 点"保存"按钮后右上角"✓ 已保存"绿色提示出现
- ✅ 2.2 秒后自动消失
- ✅ 帧内占用 100% 时进度条变红

**对应文档**：
- P0 Bug-4 el-radio label 全局替换
- P1 Issue-4 协议页头部 6 元素横排
- P2 Issue-7 协议页结构预览条
- P2 Issue-10 字段行内编辑反馈弱

---

### 界面 5：TestData 测试数据（commit `16c7a22`）

**改进**：
- 数据矩阵工具栏左侧加 3 个统计 tag：
  - 列数：字段列数（协议/接口关联字段）
  - 总格数：行 × 列
  - 已编辑：相对默认值（0/空串）有变化的单元格数（黄色 warning，0 时不显示）
  - 超限：超出约束范围（range/enum）的单元格数（红色 dark，0 时不显示）
- 演示者一眼能看到当前数据集的填写进度与异常，不必展开所有行

**Playwright 验证**：
- ✅ 数据集"帧控制字节-全场景" 4 行 × 7 列 = 28 格，已编辑 11
- ✅ 没有 outOfRange 数据集（故红色 tag 不显示）

**对应文档**：
- 总体评估 1.1 TestData 评级 B+ 提升路径

---

### 界面 6：TestTask 任务管理（commit `43f0943`）

**改进**：
- 按 P1 Issue-3 重构任务详情头：
  - 第一行只保留「任务名输入」+ 「提交执行/复制/删除」3 个按钮
  - 状态/优先级 tag 下移到第二行（meta 区）
  - meta 区顶部 1px dashed 虚线分隔
  - 加分割竖线，视觉分组更清晰
  - 新增「已执行 N 次」统计（从 currentTask.runs.length 取）
- Bug-4 状态：该界面相关组件（CreateTaskDialog / TaskStrategy / MqTestConfig）
  全部已用 el-radio `:value=` 写法，无需修改

**Playwright 验证**：
- ✅ 任务头不再挤在一行，任务名宽度增加
- ✅ meta 区显示「执行中」+「中」+「武器管理模块」+「创建于...」+「更新于...」

**对应文档**：
- P1 Issue-3 测试任务头行 5 元素挤一行

---

## 3. 验证与质量保证

### Playwright 走查覆盖

| 界面 | 视口 | 关键操作 | Console warnings | Console errors |
|---|---|---|---|---|
| Login | 1920×1080 | 点击 4 个 demo 账号、自动填充、登录 | 0 | 0 |
| Dashboard | 1920×1080 | 切换系统、修改 store 验证联动 | 0 | 0 |
| Connection | 1920×1080 | 切系统 chip、检测连通性 | 0 | 0 |
| Protocol | 1920×1080 | 切类型、切字节序、保存（提示出现/消失） | 0 | 0 |
| TestData | 1920×1080 | 选数据集、看统计 tag | 0 | 0 |
| TestTask | 1920×1080 | 选任务、看新任务头布局 | 0 | 0 |

### 回归测试

未对其他 5 个界面（Execution / Rule / Exception / Statistics / KnowledgeModel / Report / Settings）
做主动修改，但 Playwright 打开的 snapshot 显示这些界面仍正常渲染（路由切换 + 公共组件未破坏）。

### 临时文件清理

每完成一个界面后都执行：
```powershell
Get-ChildItem -LiteralPath .playwright-mcp -File |
  Where-Object { $_.Name -notlike 'shots*' } |
  Remove-Item -Force
```
`.playwright-mcp/shots/` 目录保留（截图证据）。

`AGENTS.md` 已更新，**后续每次 Playwright 走查后自动清理临时文件**。

---

## 4. 待办（v3.1 后续消化）

文档中已识别但本次未做（按 ROI 不在前六界面必修范围）：

| # | 项目 | 文档位置 | 原因 |
|---|---|---|---|
| 1 | Bug-2 Settings 角色字段空白 | P0 | 不在"前六界面"内 |
| 2 | Issue-2 KnowledgeModel 行点击 | P1 | KnowledgeModel 在第 6 之后 |
| 3 | Issue-5 执行编排底部"就绪检查"分离 | P1 | Execution 在第 7 |
| 4 | Issue-8 统计页表格行点击无反应 | P2 | Statistics 在第 10 |
| 5 | Issue-9 报告"重新生成"行为不明确 | P2 | Report 是附 1 |
| 6 | Issue-11 `被测系统` 下拉 computed 7 处重复 | P3 | 跨页重构 |
| 7 | Issue-12 状态/优先级映射函数 5+ 处重复 | P3 | 跨页重构 |
| 8 | Issue-13 PanelCard 抽公共组件 | P3 | 跨页重构 |
| 9 | 视角 2 键盘快捷键 + Ctrl+K | 长期 | 1.5d 工作量 |
| 10 | 视角 3 按需引入图标 | 长期 | 性能优化 |
| 11 | 视角 6 localStorage 持久化 | 长期 | 演示连续性 |
| 12 | 视角 8 错误边界 | 长期 | 健壮性 |

---

## 5. 一句话总结

6 个界面优化完成：
- **3 个 P0 Bug 全部修复**（头像取字、BrokerHealthPanel 字符串模板、FramingPanel el-radio）
- **3 个 P1 Issue 全部实施**（Connection 系统选择器 chip 化、TestTask 头布局、Protocol 头部精简）
- **3 个 P2 Issue 全部实施**（跨页 event bus 联动、协议结构预览条、保存反馈）
- **6 个 commit 全部已落到本地 git**，未推远端（按用户要求）
- **0 warnings / 0 errors** 在所有 6 个界面
- **AGENTS.md** 已加 Playwright 临时文件清理规则
